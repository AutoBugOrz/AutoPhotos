package com.autophotos.utils;

import java.awt.*;

/**
 * @author: LoungeXi
 **/
public final class HomePage {
    public static final Double WIDTH = Toolkit.getDefaultToolkit().getScreenSize().getWidth() * 0.82;
    public static final Double HEIGHT = Toolkit.getDefaultToolkit().getScreenSize().getHeight() * 0.82;

    private HomePage() {
    }

}
